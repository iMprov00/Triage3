# Скопируйте в .env и задайте секреты перед первым запуском:
#   copy .env.example .env

PUBLIC_HOST=192.168.1.184
STAGE1_PORT=1001
STAGE2_PORT=1002

# rails secret  или  openssl rand -hex 64
SECRET_KEY_BASE=change-me-generate-with-rails-secret

# содержимое api/config/master.key (одна строка)
RAILS_MASTER_KEY=
