<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
  <title>Триаж — Госпитальная система</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/app.css">
</head>
<body>
  <%# Верхняя полоса: навигация %>
  <header class="app-header">
    <div class="app-container container d-flex align-items-center justify-content-between">
      <div class="d-flex align-items-center gap-3">
        <a href="/patients" class="app-logo">Триаж</a>
        <a href="/patients" class="app-nav-btn"><i class="bi bi-people"></i> К списку пациентов</a>
      </div>
      <a href="/monitor" target="_blank" class="app-nav-btn"><i class="bi bi-tv"></i> Монитор ТВ</a>
    </div>
  </header>

  <main class="app-container container">
    <%# Сообщения после редиректа %>
    <% if flash[:notice] %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <%= flash[:notice] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Закрыть"></button>
      </div>
    <% end %>
    <% if flash[:error] %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <%= flash[:error] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Закрыть"></button>
      </div>
    <% end %>
    <% if flash[:info] %>
      <div class="alert alert-info alert-dismissible fade show" role="alert">
        <%= flash[:info] %>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Закрыть"></button>
      </div>
    <% end %>

    <%= yield %>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
  <script>
    // Поддержка методов DELETE и PUT
    $(document).ready(function() {
      $('form[method="post"]').each(function() {
        const method = $(this).find('input[name="_method"]').val();
        if (method) {
          $(this).attr('method', 'post');
        }
      });
    });
  </script>
</body>
</html>